TARGET := gemm
CUDA_PATH := ${CUDA_PATH}
NVCC := $(CUDA_PATH)/bin/nvcc
CXXFLAGS := -std=c++11
bin_dir := bin

all: bin_

bin_: $(TARGET).cu bin_dir
	$(NVCC) $(CXXFLAGS) $(TARGET).cu -o $(bin_dir)/$(TARGET) -lcublas


.phony: clean

clean:
	rm -rf $(bin_dir)

